// Mirrors the CSS custom properties in src/app/globals.css (:root).
// globals.css remains the source of truth for styling — keep these in sync manually.
export const theme = {
  colors: {
    red: "#d71920",
    black: "#0b0b0b",
    cream: "#efe9df",
    white: "#fff",
    gray: "#777",
    line: "rgba(11,11,11,.2)",
    acid: "#f5ff35",
  },
} as const;
